# Elvate

A simple cross-platform course planner built with Expo, Expo Router, and Tamagui.

Use it to keep courses, weekly meetings, and assignments in one place. Data stays on device via AsyncStorage.

## Run

```bash
npm start
```

Then open iOS, Android, or web from the Expo CLI. For web:

```bash
npm run web
```
